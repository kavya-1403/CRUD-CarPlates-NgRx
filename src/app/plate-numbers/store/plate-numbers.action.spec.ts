import { PlateNumbersAction } from './plate-numbers.action';

describe('PlateNumbersAction', () => {
  it('should create an instance', () => {
    expect(new PlateNumbersAction()).toBeTruthy();
  });
});
