import { PlateNumbersReducer } from '../../plate-numbers/store/plate-numbers.reducer';

describe('PlateNumbersReducer', () => {
  it('should create an instance', () => {
    expect(new PlateNumbersReducer()).toBeTruthy();
  });
});
